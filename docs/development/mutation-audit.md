# Разбор мутаций

Проверено 6 сентября 2026 года. В прогоне 15 файлов логики движка и 135 тестов frontend.
Отдельный общий прогон включает ещё три HTTP-теста backend. Охват, команды и решения
по удалению слабых тестов описаны в [тестовом аудите](./testing.md).

| Статус Stryker | Количество |
| --- | ---: |
| Killed — отклонены тестами | 1480 |
| Timeout — обнаружены по лимиту времени | 30 |
| Survived — тесты не отличили | 43 |
| NoCoverage — участок не исполнен | 3 |
| Всего | 1556 |

Исходный показатель Stryker — **97,04%**, порог команды — **95%**. Показатель не
пересчитан после ручной классификации. Формального результата 100% нет: оставшиеся
варианты перечислены полностью ниже. Среди них есть непроверяемая стоимость вычислений,
диагностические тексты и различия точно на границе численного допуска; они не объявлены
безусловно эквивалентными программами.

## Что обнаружила ревизия

Исправлены ожидания и добавлены сценарии, отличающие нарушение исключительности предметов
и мест хранения, кражу чужого заказа, повторное использование ID, выбор неподходящей роли,
проход через вертикальную стену, пропуск боя при движении и неправильные остатки времени.
Проверка резервов теперь утверждает результат первого аукциона: повторные торги раньше
скрывали ошибочную выдачу только одного задания. Проверка равной стоимости различает
неверный компаратор ID, а проверка точного прибытия — задержку логической клетки на шаг.

Прежний тест предполагаемого обхода через чертежи не содержал такого пути: карта сценария
исправлена. Слишком широкий стандартный toBeCloseTo заменён явной точностью там, где он
пропускал дополнительный шаг строительства. Тесты констант подсказок и повторные сценарии
удалены; правила новой модели проверяются через команды и наблюдаемое состояние.

## Все оставшиеся варианты

ID относятся к этому прогону и изменятся при изменении исходников. NC означает NoCoverage.
Пути строк в таблице отсчитываются от apps/frontend/src/game/. Предусловие «допустимый мир»
означает состояние, полученное из createGame через команды и stepGame; произвольная запись
во внутренние поля Game этим предусловием не разрешена.

| Мутации | Классификация | Обоснование и ограничение |
| --- | --- | --- |
| #20 (cells.ts:13), #21 (cells.ts:13), #22 (cells.ts:13), #24 (cells.ts:13) | Эквивалентность runtime | Проверка Number.isInteger ниже уже отвергает undefined. Явные проверки нужны также для сужения типа TypeScript. |
| #457 (items.ts:10), #1310 (tasks.ts:163) | Объединение типов | Поле unitId существует только у местоположения carried; у cell оно undefined и не совпадает с числовым ID носителя. |
| #1371 (units.ts:30) | Невалидный тип мутанта | Строка Stryker не является видом Job и не даёт матке допустимых работ. Vitest транспилирует мутации без проверки TypeScript; исходный проект проходит отдельный type-check. |
| #734 (simulation.ts:24), #736 (simulation.ts:24, NC) | Инвариант / диагностический текст | Стартовая карта содержит достаточно разных клеток для пяти подвижных юнитов. Ошибка предназначена для несовместимого изменения стартовой конфигурации, а её текст не влияет на игру. |
| #851 (simulation.ts:104) | Инвариант шага | Ветка достижима только если матка была жива в начале шага. До этой проверки ни одна система не удаляет матку. |
| #990 (tasks.ts:25, NC), #1539 (work.ts:87) | Связность допустимого мира | Путь между защищёнными входом и домом сохраняется командами карты. Для возвращающегося разведчика поверхность связана с тем же входом. Fallback нужен только для нарушенного состояния карты. |
| #164 (combat.ts:14), #172 (combat.ts:20) | Инвариант контактов | ID уникальны: равный минимум обозначает тот же объект. Индекс клетки создан первым проходом для каждого участника второго прохода. |
| #254 (demolition.ts:10) | Инвариант карты | В допустимой колонии путь к защищённой матке не исчезает. Даже без начальной отметки вход повторно достигается из следующей клетки BFS. Это не утверждение об эквивалентности для произвольного несвязного словаря. |
| #1242 (tasks.ts:127) | Проверенное предложение | Путь доставки уже проверен при создании предложения. Карта и Navigation между проверкой и расчётом ставки не меняются. |
| #1326 (tasks.ts:167) | Неизменная идентичность | Аукцион выдаёт attack только на другую сторону. Faction и ID после создания доступны только для чтения, а живой целевой объект не заменяется другим с тем же ID. |
| #1479 (work.ts:63), #934 (spawning.ts:30) | Владение и порядок шага | Перед исполнением предмет проверен актуальностью задания; эксклюзивные резервы запрещают другому исполнителю удалить его. Заказ выведения ссылается на яйцо с уникальным ID: вид предмета не меняется на еду. |
| #776 (simulation.ts:69), #1044 (tasks.ts:45), #639 (navigation.ts:61), #1577 (work.ts:109), #1578 (work.ts:110), #1579 (work.ts:110, NC) | Одинаковый результат | Уменьшение и увеличение одного счётчика взаимно сокращаются; массив хранения налётчиков не используется; undefined и false одинаково разрешают движение; пустые ветки guard/attack завершаются без действия. |
| #592 (navigation.ts:24), #605 (navigation.ts:36) | Производительность | Отключение кеша повторяет BFS с тем же маршрутом. Поведенческий тест не должен зависеть от приватного устройства кеша. Стоимость шага проверена отдельным замером, но автоматического порога быстродействия нет. |
| #1040 (tasks.ts:45), #1226 (tasks.ts:116) | Лишние вычисления | Вычисляются неиспользуемые места хранения либо предложения при отсутствии исполнителей. Последний вариант также расходует дополнительный RNG: точное число вызовов случайности не является игровым контрактом. Это не строгая эквивалентность последовательности случайных чисел. |
| #1422 (work.ts:24), #1424 (work.ts:24), #1425 (work.ts:24), #1427 (work.ts:24), #1428 (work.ts:24), #1430 (work.ts:24) | Пересчёт погони | Актуальность задания уже гарантирует живую цель. Варианты повторно строят тот же путь с сохранением начатого ребра. Если цель в текущей клетке, фаза контактов того же шага всё равно очищает путь. Число вызовов поиска не закреплено тестом. |
| #200 (combat.ts:35), #214 (combat.ts:44), #247 (construction.ts:28), #417 (eggs.ts:25), #948 (spawning.ts:33), #1406 (work.ts:19), #1535 (work.ts:85), #648 (navigation.ts:63), #657 (navigation.ts:67) | Граница численного допуска | Меняется только строгость сравнения ровно на границе EPSILON (1e-9). Это не математически идентичные программы. Контракт допуска не закрепляет такую границу; тесты проверяют обычные границы периодов, точное прибытие и остатки шагом 0,03 с. |
| #808 (simulation.ts:83), #281 (demolition.ts:25) | Диагностика отказа | Некорректный шаг по-прежнему бросает RangeError до записи; координаты вне допустимой карты по-прежнему отклоняются. Меняется текст сообщения, а не возможность выполнить команду. |

## Артефакты

Полные замены кода, покрывающие тесты и статусы находятся в .tmp/mutation/report.json;
интерактивная версия — .tmp/mutation/report.html. Они создаются командой pnpm test:mutation
и не хранятся в Git. Конфигурация сохраняет все виды мутаций; общих исключений для строк,
сравнений или защитных условий нет. Корневая .tmp не копируется во временный проект.

SHA-256 снимка 15 файлов из отчёта (отсортированные пары «путь, NUL, текст, NUL»):

~~~text
c2976df08b219504deaacba37664d6b03b626099636d3a776e96850a672747be
~~~
